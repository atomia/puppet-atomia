# atomiadns_ns.yaml
# Defines classes and variables needed to
# deploy a nameserver and connect it with an
# Atomia DNS master.
---
classes:
  - atomia::atomiadns_powerdns

atomia::atomiadns_powerdns::agent_user: "atomiadns"
atomia::atomiadns_powerdns::agent_password: "PassfromMDNS"
atomia::atomiadns_powerdns::atomia_dns_url: "http://192.0.2.110/atomiadns"