# cronagent.yaml
# Defines classes and variables needed to
# deploy a cron agent 
---
classes:
  - atomia::cronagent
  - atomia::nagios::client

atomia::cronagent::global_auth_token: "token123"
