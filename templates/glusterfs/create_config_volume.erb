<%
replica_str = @peers_size >= 2 ? "replica ${peers_size} " : ""
p_str = ""
@peers_arr.each do |p| 
	p_str = p_str + p + ':/export/config/vol1 '
end%>/usr/sbin/gluster volume create config_volume <%=replica_str %><%=p_str%>
